import { StyleSheet } from "react-native";

const FontStyle = StyleSheet.create({
    h1: {
        fontSize: 35,
        fontWeight: 'bold'
    },
    textButton: {
        fontSize: 16,
        fontWeight: 'bold'
    },
})

export default FontStyle